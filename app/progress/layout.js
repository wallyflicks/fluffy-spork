export const metadata = {
  title: 'Your Progress — Orivox',
  description: 'Track your speaking improvement over time. View your session history, streaks, scores, and achievements all in one place.',
}
export default function Layout({ children }) { return children }
